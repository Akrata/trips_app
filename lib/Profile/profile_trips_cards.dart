import 'package:flutter/material.dart';

class ProfileTripsCards extends StatelessWidget {
  const ProfileTripsCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final tripImage = "";

    return Container();
  }
}
